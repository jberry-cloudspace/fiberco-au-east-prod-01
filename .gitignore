# Host-level secrets
.env

# Project environment files (contain ports, may contain secrets)
docker/projects/*/.env

# Keep the projects directory structure
!docker/projects/.gitkeep

# Basecamp is its own repo (jberry-cloudspace/basecamp-code)
basecamp/

# Large database dumps
*.dump
